name: Backend application build and test
on:
  workflow-dispatch:
    
jobs:
  build-and-test-job:
    runs-on: ubuntu-latest
    steps:
      - name: going to build
        run: mvn clean install -DskipTests=true
      - name: testing
        run: mvn test



