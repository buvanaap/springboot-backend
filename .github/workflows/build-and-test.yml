name: Backend application build and test
on:
  workflow_dispatch:

jobs:
  build-and-test-job:
    runs-on: ubuntu-latest
    steps:
      - name: build panna porom
        run: mvn clean install -DskipTests=true
      - name: test panna poren
        run: mvn test
